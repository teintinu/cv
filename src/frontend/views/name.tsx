import styled from 'styled-components';

export const Name = styled.div`
  font-family: Sofia;
  font-size: 3em;
  padding: 10px;
  top: 0px;
  height: 70px;
  width: 100%;
  position: fixed;
  background-color: var(--primaryBg);
  color: var(--primaryFg);
  display: grid;
  place-items: center;
`;
